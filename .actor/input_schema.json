{
    "title": "UK Solicitor Directory Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "List of search URLs to scrape.",
            "editor": "requestListSources",
            "prefill": [
                { "url": "https://solicitors.lawsociety.org.uk/search/results?Type=0&IncludeNlsp=True&ProBonoOnly=False" }
            ]
        },
        "maxLeads": {
            "title": "Maximum Leads",
            "type": "integer",
            "description": "Maximum number of UK solicitor leads to extract.",
            "default": 100,
            "minimum": 1
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Proxy settings. Apify Residential Proxy (UK targeted) is required.",
            "prefill": { "useApifyProxy": true },
            "editor": "proxy"
        }
    },
    "required": ["startUrls"]
}
